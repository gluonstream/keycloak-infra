# 1. Create the Pool (Internal Storage Bucket)
apiVersion: ceph.rook.io/v1
kind: CephBlockPool
metadata:
  name: xena-replicapool
  namespace: rook-ceph
spec:
  failureDomain: host
  replicated:
    size: 3 # Replicate data 3 times across your 4 nodes
---
# 2. Create the StorageClass (The Dynamic Provisioner)
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: rook-ceph-block
provisioner: rook-ceph.rbd.csi.ceph.com
parameters:
  clusterID: rook-ceph # namespace where rook is installed
  pool: xena-replicapool
  imageFormat: "2"
  imageFeatures: layering
  csi.storage.k8s.io/provisioner-secret-name: rook-csi-rbd-provisioner
  csi.storage.k8s.io/provisioner-secret-namespace: rook-ceph
  csi.storage.k8s.io/controller-expand-secret-name: rook-csi-rbd-provisioner
  csi.storage.k8s.io/controller-expand-secret-namespace: rook-ceph
  csi.storage.k8s.io/node-stage-secret-name: rook-csi-rbd-node
  csi.storage.k8s.io/node-stage-secret-namespace: rook-ceph
  csi.storage.k8s.io/fstype: xfs # Using XFS for performance, matching your Debian setup
allowVolumeExpansion: true
reclaimPolicy: Delete
